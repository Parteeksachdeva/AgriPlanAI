# Point frontend to backend API (run backend with: python3 -m uvicorn main:app --host 0.0.0.0 --port 8000)
# Frontend will call VITE_API_URL + '/predict' (e.g. http://localhost:8000/predict)
VITE_API_URL=http://localhost:8000
